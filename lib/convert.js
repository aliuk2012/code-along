// provide a function that will take an entered script, and generate
// injectable code
// import buble from 'buble';
// var buble = require('buble')

// import {rollup} from 'rollup';
import {foo} from 'bar';
import {select} from 'd3';

export default function convert (code) {

  select('foo')

  d3.select("BAS")

  return foo()
  //transform(code)

}
